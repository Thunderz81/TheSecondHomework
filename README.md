# 郭俊炜-25306020-第二次人工智能编程作业
## 1. 任务拆解与 AI 协作策略
将大任务按一个个需要实现的功能拆解与AI协作
步骤1：建立Student类
步骤2：完成考生信息表的读入与初始化
步骤3：完成学生学号查询功能
步骤4：完成随机点名功能
步骤5：完成考场座位表生成功能
步骤6：完成准考证生成功能
步骤7：将查询、随机点名、考场安排表生成、准考证生成等功能封装在ExamSystem类里
步骤8：为核心代码块添加必要的注释
## 2. 核心 Prompt 迭代记录
初代 Prompt：在输入学号查询的实现中，AI的实现只允许输入一次，如果输入了不符合要求的学号就会直接跳过进入随机点名环节
优化后的 Prompt (追问)：增加了循环判定，如果输入的学号不符要求，则控制重新输入，直到符合要求为止，再输出对应的学生信息
## 3. Debug 与异常处理记录
其中一次异常：输入随机点名的人数时，输入了小写字母，程序提示报错
解决过程：通过仔细阅读Traceback返回的文字信息，对应相应的错误行，结合返回的错误类型：ValueError，确定具体的错误，手动进行修改，之后再运行、调试，确保万无一失
## 4. 人工代码审查 (Code Review)
```python
# 贴入代码及人工注释
def random_roll_call(self, count): #随机点名功能，输入要点名的学生数量，返回随机选中的学生列表
        if count <= 0: #输入验证，确保点名数量为正整数
            raise ValueError("请输入大于 0 的整数。") #返回错误提示
        if count > len(self.students): #输入验证，确保点名数量不超过学生总数
            raise ValueError(f"输入数量超过学生总数（{len(self.students)}），请重新输入。")
        return random.sample(list(self.students.values()), count) #随机选择指定数量的学生
```

